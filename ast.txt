AST
|- VAR DECL
    |- NAME: x
|- VAR ASSIGN
    |- NAME: x
    |- VALUE: 10

IR
__root__: (ir function)
    define_var int x (ir inst = variable decl (x))
    store x 10 (ir inst = variable assign)
    exit 0 (ir inst = standart exit)

ASM
```
BITS 64
segment .text

global _start
_start:
    ; CODE
    mov QWORD [x], 10

    ; Call exit procedure
    mov rdi, rax
    mov rax, 60
    syscall

segment .bss
x: resq 1
```